
{
  "name": "android-device-monitor",
  "version": "1.0.0",
  "description": "Real-time Android device monitoring via ADB",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "watch": "tsc --watch",
    "monitor": "node dist/index.js --interval 2000"
  },
  "dependencies": {
    "chalk": "^4.1.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
